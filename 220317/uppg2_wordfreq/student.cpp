#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

// Uppgift 2: Implementera klassen 'word' och funktionen 'read_words'.
// 'word' ska hålla en sträng och en räknare (frekvens).
// 'read_words' ska läsa ord för ord och hålla vektorn sorterad alfabetiskt.

class word {
public:
    // Tips: Konstruktor, get_word(), get_freq() och operator++() behövs.
};

// Tips: Du behöver en operator< för att kunna sortera/söka i vektorn.

std::vector<word> read_words(std::istream& is) {
    // Din kod här. Använd gärna std::lower_bound för att hitta rätt plats!
}